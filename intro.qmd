# Introduction



```{r}
#| echo: false
#| eval: false

con <- DBI::dbConnect(RSQLite::SQLite(), 
                      dbname = ":memory:")

dplyr::copy_to(dest = con, 
        df = mtcars, 
        name = "mtcars")

DBI::dbListTables(con)
```
<!-- 
    ``` {.sql}
    #| connection: con
    #| 
    
    SELECT *
    FROM mtcars
    ```
-->

<!--- TODO coś innego -->
